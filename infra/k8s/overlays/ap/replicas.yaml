# Capacity is sized per region to its actual traffic, not copied from us-east.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: helix-api
spec:
  replicas: 4
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: helix-api
spec:
  minReplicas: 4
  maxReplicas: 60
